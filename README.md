# udp_server_package
A lightweight Python package that provides a simple UDP server implementation. It listens on a specified port, receives raw UDP packets, and prints incoming data in hexadecimal format—ideal for debugging, embedded systems communication, and network data inspection.
